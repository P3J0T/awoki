# Awoki session state

Runtime session attachment files live here.

- `session_project.json` is session-scoped and only valid for the current Awoki MCP process.
- `last_project.json` is informational and must not auto-attach a new OpenCode session.
