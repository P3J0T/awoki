---
description: Show statically resolved callees of a symbol
---

Use the `project-continuity` skill. Call `code_callees(symbol="$ARGUMENTS")`.
Label resolved, ambiguous, and unresolved calls and preserve the active branch.
