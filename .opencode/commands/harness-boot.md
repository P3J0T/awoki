---
description: Orient the agent inside Awoki
---

Call `harness_status`, then `load_manifest` if needed. Search skills for `$ARGUMENTS`, then call `recall_context` with the user's current direction or "project bootstrap" if no arguments were supplied. Explain the active project, memory scopes, skill candidates, and safety mode before doing work.
