---
description: Demote a global memory into this project scope
---

Use `search_global_memory` to find the target memory, then call `demote_global_memory` with the global memory line number and a reason. Demotion is append-only: it copies the useful text into project memory and hides that global line from future global recall.
