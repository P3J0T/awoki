---
description: Show statically resolved callers of a symbol
---

Use the `project-continuity` skill. Call `code_callers(symbol="$ARGUMENTS")`.
Label resolved, ambiguous, and unresolved call sites. Do not claim dynamic calls
were excluded unless the language/runtime makes that provable.
