---
description: Start an explicit localhost-only Lavish review for a project HTML artifact
---

Load the `lavish-review` skill and prepare an ad-hoc visual review for: $ARGUMENTS

Use only `workspace/.lavish/current/` as the staged review area. Start Lavish with `--no-open`, print the host URL, and instruct the user to run `./open-lavish.sh` on macOS. Do not install ambient hooks and do not use `lavish-axi share` unless explicitly requested.
