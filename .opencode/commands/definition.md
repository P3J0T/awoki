---
description: Resolve an exact code symbol definition
---

Use the `project-continuity` skill. Call `code_definition(symbol="$ARGUMENTS")`.
Do not substitute semantic similarity for an exact definition. Report ambiguity
instead of choosing arbitrarily.
